import Core'Number
import Core'String

import 'entity { Entity }
import 'entity'frog { Frog }
import 'entity'snake { Snake }
import 'field { Field }
import 'game { Game }
import 'position { Position }
import 'usage { Usage }

empty = \() string { "." }

Render = \(g Game) string {
  f = game'Field(g)

  String'Concatenate(
    [string
      "\x1bc",
      String'Join(
        [string
          String'Concatenate(
            [string
              if c = game'EntityAt(g, Position{X: x(), Y: y()}) as none {
                empty()
              } else {
                renderEntity(c)
              }
              for x in Number'Sequence(f.Columns)
            ],
          )
          for y in Number'Sequence(f.Rows)
        ],
        "\r\n",
      ),
      "\r\n",
      Usage(),
      "\r\n",
    ],
  )
}

renderEntity = \(c Entity) string {
  if _ = c as Snake {
    "o"
  } else if Frog {
    "*"
  }
}
