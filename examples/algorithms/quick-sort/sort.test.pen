import Test'Assert

import 'sort

TestSortNothing = \() none | error {
  Assert'Equal(sort'Sort([number]), [number])
}

TestSortNumbers = \() none | error {
  Assert'Equal(sort'Sort([number 3, 1, 2]), [number 1, 2, 3])
}
