[package]
name = "pen-unix"
version = "0.1.0"
publish = false
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
ffi = { package = "pen-ffi", version = "*", features = ["runtime", "std"] }
tokio = { version = "1", features = ["full"] }
