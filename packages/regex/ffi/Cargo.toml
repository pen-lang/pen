[package]
name = "ffi"
version = "0.1.0"
publish = false
edition = "2024"

[lib]
crate-type = ["staticlib"]

[dependencies]
ffi = { package = "pen-ffi", version = "*", features = ["std"] }
regex = "1"
