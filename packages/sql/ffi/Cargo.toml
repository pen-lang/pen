[package]
name = "pen-sql"
version = "0.1.0"
publish = false
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
ffi = { package = "pen-ffi", version = "*", features = ["runtime", "std"] }
futures = "0.3"
sqlx = { version = "0.7", features = [
  "any",
  "mysql",
  "postgres",
  "runtime-tokio",
  "sqlite",
  "tls-rustls",
] }
