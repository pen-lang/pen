[package]
name = "os-app"
version = "0.1.0"
publish = false
edition.workspace = true

[dependencies]
async-stream = "0.3"
ffi = { package = "pen-ffi", version = "*", features = ["runtime"] }
futures = "0.3"
tokio = { "version" = "1", features = ["full"] }
