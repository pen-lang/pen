import Test'Assert

import 'Map'NumberMap

get = \(m NumberMap'Map, k number) number | none {
  if v = NumberMap'Get(m, k, none) as number {
    v
  } else {
    none
  }
}

TestMapSet = \() none | error {
  k = 42
  v = 2045

  Assert'True(get(NumberMap'Set(NumberMap'New(), k, v), k) == v)
}
