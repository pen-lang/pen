[package]
name = "interface"
version = "0.1.0"
publish = false
edition = "2024"

[dependencies]
hir = { path = "../hir" }
position = { path = "../position" }
serde = { version = "1.0.229", features = ["derive", "rc"] }
