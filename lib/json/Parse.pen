import Core'String
import 'Value { Value }

Parse = \(s string) Value | error {
  if s == "null" {
    Value{Value: none}
  } else if s == "true" {
    Value{Value: true}
  } else if s == "false" {
    Value{Value: false}
  } else {
    error("not implemented")
  }
}
