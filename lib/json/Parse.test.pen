import Test'Assert
import 'Parse { Parse }
import 'Value

ParseNull = \() none | error {
  Assert'True(Parse("null")?.Value == none)
}

ParseTrue = \() none | error {
  Assert'True(Parse("true")?.Value == true)
}

ParseFalse = \() none | error {
  Assert'True(Parse("false")?.Value == false)
}
