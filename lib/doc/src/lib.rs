use ast::*;

pub fn generate(_module: &Module, _comments: &[Comment]) -> String {
    todo!()
}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
